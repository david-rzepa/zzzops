# Pending goal-template changes

Generated by `$install-zzzops` under `.zzzops/migration/template-diffs/<old-hash>--<new-hash>.md`. `$migrate-zzzops-todos` reviews it against state, applies only necessary preservation-aware edits, asks for new project input when required, and removes this file when resolved.

```diff
No pending template changes.
```
